{
  "name": "@fulbito/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "imports": {
    "#http": "./src/http.ts"
  },
  "scripts": {
    "dev": "node --import tsx --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --import tsx src/index.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "echo 'No tests for @fulbito/api'",
    "lint": "echo 'No lint config for @fulbito/api yet'"
  },
  "dependencies": {
    "@fulbito/api-contracts": "workspace:*",
    "@fulbito/domain": "workspace:*",
    "@fulbito/server-core": "workspace:*",
    "hono": "^4.7.2"
  },
  "devDependencies": {
    "@hono/node-server": "^1.14.0",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
